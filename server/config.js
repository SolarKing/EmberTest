module.exports = {
  'secret': 'casitservices', // used when we create and verify JSON Web Tokens
  // the URI with username and password to your MongoDB installation
  'database': 'mongodb://localhost/MEEN'
}
